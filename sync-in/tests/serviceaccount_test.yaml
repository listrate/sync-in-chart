suite: serviceaccount
templates:
  - templates/serviceaccount.yaml
tests:
  - it: renders service account
    set:
      mariadb.rootPassword: testpass
    asserts:
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: RELEASE-NAME-sync-in
